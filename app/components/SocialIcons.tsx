import XIcon from './XIcon';

export function SocialIcons() {
  return (
    <div className="flex gap-4">
      <XIcon
        url="https://twitter.com/incentiveiq"
        className="text-[rgb(var(--primary))] hover:opacity-80 transition-opacity"
      />
    </div>
  );
}
